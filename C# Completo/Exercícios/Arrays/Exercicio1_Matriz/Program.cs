namespace Course {
    class Program {

        static void Main(string[] args) {

            

            /*string[] line = Console.ReadLine().Split(" ");
            int q_lin = int.Parse(line[0]);
            int q_col = int.Parse(line[1]);


            double[,] mat = new double[q_lin, q_col];

            for (int lin = 0; lin < q_lin; lin++) {

                string[] values = Console.ReadLine().Split(" ");

                for (int col = 0; col < q_col; col++) {

                    mat[lin, col] = double.Parse(values[col]);

                }
            }

            Console.Write("Digite o número que quer buscar: ");
            int x = int.Parse(Console.ReadLine());

            for (int lin = 0; lin < q_lin; lin++) {

                for (int col = 0; col < q_col; col++) {

                    if (mat[lin, col] == x) {

                        Console.WriteLine($"Posição {lin}, {col}:");

                        if (col > 0) {
                            Console.WriteLine($"Esquerda: {mat[lin, col - 1]}");
                        }

                        if (col < q_col - 1) {
                            Console.WriteLine($"Direita: {mat[lin, col + 1]}");
                        }

                        if (lin > 0) {
                            Console.WriteLine($"Cima: {mat[lin - 1, col]}");
                        }

                        if (lin < q_lin - 1) {
                            Console.WriteLine($"Baixo: {mat[lin + 1, col]}");
                        }

                    }

                }


            }*/

        }
    }
}
