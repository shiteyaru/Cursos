using System;

namespace MyApp // Note: actual namespace depends on the project name.
{
    internal class Program {
        static void Main(string[] args) {

            /*
            
            ÁLCOOL, GASOLINA E DIESEL
            
            Console.WriteLine("Digite o número referente ao tipo de combustível que deseja: \n" +
                "1. Álcool\n" +
                "2. Gasolina\n" +
                "3. Diesel");

            int combustivel = int.Parse(Console.ReadLine());
            int alcool = 0;
            int gasolina = 0;
            int diesel = 0;

            while (combustivel !=4) {
                

                if (combustivel == 1) {
                    alcool++;
                }

                else if (combustivel == 2) {
                    gasolina++;
                }

                else if (combustivel ==3) {
                    diesel++;
                }

                combustivel = int.Parse(Console.ReadLine());
            }

            Console.WriteLine(
                    "MUITO OBRIGADO!\n" +
                    $"Álcool: {alcool}\n" +
                    $"Gasolina: {gasolina}\n" +
                    $"Diesel: {diesel}");

            
            
            PLANO CARTESIANO
            
            Console.WriteLine("Escreva as coordenadas X e Y, separando-as com espaço:");
            string[] coordenadas = Console.ReadLine().Split(" ");

            int x = int.Parse(coordenadas[0]);
            int y = int.Parse(coordenadas[1]);


            while (x != 0 && y != 0) {

                

                if (x > 0 && y > 0) {
                    Console.WriteLine("Primeiro");

                }

                else if (x < 0 && y > 0) {
                    Console.WriteLine("Segundo");
                }

                else if (x < 0 && y < 0) {
                    Console.WriteLine("Terceiro");
                }
                else {
                    Console.WriteLine("Quarto");
                }

                coordenadas = Console.ReadLine().Split(" ");

                x = int.Parse(coordenadas[0]);
                y = int.Parse(coordenadas[1]);


            }

            


            
            
            VERIFICAR SENHA
            
            Console.WriteLine("Digite a senha:");
            int senha = int.Parse(Console.ReadLine());

            while (senha != 2002) {

                Console.WriteLine("Senha Invalida! Digite a senha correta.");
                senha = int.Parse(Console.ReadLine());
            }

            Console.WriteLine("Acesso Permitido");
            */
        }
    }
}
